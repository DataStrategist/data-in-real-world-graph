# Don't commit a real .env. Put secrets in Netlify UI instead.
# This file is only for local testing (netlify dev) with placeholder values.

NEO4J_URI=neo4j+s://c28327ff.databases.neo4j.io
NEO4J_USER=neo4j
NEO4J_PASSWORD=REPLACE_ME
NEO4J_DATABASE=neo4j

# Optional safety/cost controls
NODE_LIMIT=200
ROW_LIMIT=1200
CACHE_SECONDS=300
